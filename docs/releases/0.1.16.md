Fixes the wave ball exposing a trough during animation when quota is full. Preserves the floating bar on external displays, five-hour quota, and hover to restore. Updates asset loading in the installed app and activity detection.
