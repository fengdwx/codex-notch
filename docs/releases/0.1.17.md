Fixes the floating bar disappearing when screen mirroring is enabled. When the primary mirrored display has no physical notch, the bar now appears at the top center and is positioned using that display's current dimensions.

- Retains the v0.1.16 fixes for five-hour quota, hover to restore after hiding, animation asset loading, and the wave ball at full quota.

Downloads are available for Apple Silicon (arm64) on macOS 14 or later. DMG, ZIP, and their SHA-256 checksum files are provided.

The app uses an ad-hoc signature and has not been notarized by Apple. The DMG does not eliminate Gatekeeper prompts.
